;; extends

;; Undefined is highlighted as @variable.builtin by default. Strictly
;; speaking, it is not a reserved word and just a built-in variable,
;; but no one re-assign it.
(undefined) @constant.builtin
